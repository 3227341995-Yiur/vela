def main() -> None {
    mut n: int = 8
    mut a: Array[int, 8]
    mut out: Array[int, 8]
    for i in range(0, n) {
        a[i] = i
    }
    parallel for i in range(0, n) {
        out[i] = a[i] * 2
    }
    print(out[7])
}
