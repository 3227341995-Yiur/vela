# Vela —— 北极星，以及通往它的路

[English](ROADMAP.md) | **简体中文**

<!--
源文件 : ROADMAP.md
源文件字节 : 18004
源文件 SHA256 : 858b83c1e03a6da1be121488e8424d8aa899004b99e818b7dd56ceed5699dc5e
翻译日期 : 2026-09-22
规则 : 本文件是上面那个英文文件的完整翻译。英文文件一旦改动，本文件立即过期，
       powershell -ExecutionPolicy Bypass -File tools\docs-zh-check.ps1 会指名报告。
-->

目标，按这门语言的所有者的说法：

> Vela 是一门独立的、系统级的、高级编程语言。它是纯血的：它不从别的语言那里借用工具链。它应该**比 C++ 更快**，应该具备 **Rust 的安全设计**，应该具备 **Python 的语法与严格的语义**——并且拥有**一门高级语言的完整特性集**。

本文件把它变成带验收标准的工作，因为其中的每一个子句要么是可测量的，要么就只是一句口号。有两条约束塑造了整个顺序：

1. **编译器是用 Vela 写的，而且必须持续编译它自己。** 这里的每一个特性都必须*用它自己的语言*实现，由一个本身还没有那个特性的编译器来实现。这让顺序成为承重结构：一个能让编译器变简单的特性，应该排在编译器只能盲写实现的特性之前。
2. **没有被测量的断言不存在。** 这个项目已经为这一课反复付过账——一个写进文档却并不存在的 `--fast-int` 开关、被写成"已被证明消除"而每一处检查都照旧发出的边界与溢出检查、一个从未被发出过的 `restrict` 子句、两个静默地什么都不做的注册。下面每一项都点名了判定它的命令。

## 四条断言的现状，实测

| 断言 | 今天 | 差距 |
|---|---|---|
| **比 C++ 更快** | **两条 matmul 行都没能成立，而且差距是实测的：** 串行 matmul 512² **0.5433 s 对 C++ 孪生版本的 0.2241 s（慢 2.4×）**，并行 matmul **0.0680 s 对 0.0221 s（慢 3.1×）**，mandelbrot 精确到微秒打平，sieve 对着一行显式*未检查*的 C++ 慢 1.5×——见 `bench/RESULTS.md` | 落后来自仍然*留在内层循环里*的带检查算术：`vm.exe emit-c bench/matmul.vel` 编译出 6 处 `vela_bounds_check`、7 处 `vela_add_range`、3 处 `vela_mul_range` 调用（2026-09-22 实测；这里早先引用过的计数是 6/10/6）。**目前还没有任何检查被证明消除**——区间机制今天能证明的范围只限于 `parallel for`，而 `SPEC.md` §6.1 里那两行普通循环也诚实地标着"设计好了，尚未实现" |
| **Rust 的安全设计** | 没有指针、没有 `unsafe`、没有 `free`，只有 arena，下标带检查，整数算术带检查，而且 `parallel for` 的别名规则已经**完整**：跨迭代的读被拒绝，`out[i] = a[i] * 2` 与 `a[i] = a[i] + 1` 仍然合法。那五个证明它的用例在 `tests/cases.txt` 里，并且带着**已被记录的输出**——`parallel_alias_cross_iteration` 是一个 `refuse` 用例，它的 `.err` 保存着那条消息；另外四种合法形状是 `run` 用例，它们的 `.out` 保存着答案——所以这条规则是*写下*了，而且背后有五个被记录的判定 | 仍然欠着**两件不同的事**，而这个格子过去只点名了其中一件：**证明机制**还没建起来（在有证明的地方，这些检查还没有被*证明消除*），而且除了 `parallel for` 之外没有并发。把"完成别名规则"读成"规则还没写"，是读错了——没有完成的是消除检查的那个证明 |
| **Python 的语法，严格的语义** | 基于花括号、静态类型、没有真值性、`/` 拒绝整数、字符串 `+` 被拒绝、显式的 `mut`、作用域规则 | 类型系统仍有洞：绑定到 `int` 的 `str` 被 `check` 接受；未声明的类型名被 emitter 的 panic 而不是被诊断抓住 |
| **纯血 / 独立** | 编译器的源码是 Vela；链接器是 Vela；任何地方都没有 Python、没有 C++；`selfhost_fixpoint` 通过 | **`build` 仍然需要 `cl.exe`**：C 就是代码生成后端。这就是剩下的宿主语言依赖，而 LLVM 那条工作线就是移除它的东西 |

## 工作线 1 —— 后端，以及"比 C++ 更快"

有序排列，每一步都能单独验证。

1. **LLVM 第一阶段**：`vm.exe emit-llvm f.vel` 写出 IR 文本；`clang-cl`/`llc` 汇编并链接它。移除 MSVC。验收：由两个后端分别构建的 `hello.vel` 打印相同的字节；一个由 LLVM 构建的语料 `run` 用例等于 C 构建版，也等于解释器。
2. **LLVM 第二阶段**：`runtime/vela_llvm_shim.c`（架在 `llvm-c` 之上的标量接口）链接进 `vm.exe`，由它自己写出 `.obj`。验收：不需要外部编译器；只需要一个链接器。
3. **由证明驱动的检查消除**——*真正的性能杠杆*，而且它完全不需要 LLVM。检查器的区间分析已经存在；缺的是让 emitter 去消费它。验收，今天就可测：`vm.exe emit-c bench/matmul.vel` 在内层循环里含有**零**处 `vela_bounds_check` 与 `vela_add_range` 调用，*程序打印相同的答案*，而与 C++ 的 `restrict+omp` 构建的并行对比从**慢 3.1×**（2026-09-22 实测：0.0680 s 对 0.0221 s，`bench/RESULTS.md`）变成取胜或打平。如果消除并不能让数字移动，就直说。**这一条早先引用的基准——"从 −17% 出发"——已经不是那个测量值**；两个数字之所以不一致，是因为它们取自 2026-09-19 那处 emitter 修复的两侧。
4. **LLVM 后端上的 `parallel for`**，经由 OpenMP 运行时的 ABI 或我们自己的线程池。在那之前，LLVM 后端会带着一条消息拒绝它，因为一个自称并行却串行运行的循环，是这个项目绝不能交付的东西。
5. **新后端上的不动点**：经由 LLVM 构建的编译器，发出的 C 与经由 MSVC 构建的编译器逐字节相同。这就是让"带真实后端的纯血"成为事实的那个里程碑。

## 工作线 2 —— 语言的特性集

这个顺序的选择，是为了让每个特性都让*下一个*特性对一个用 Vela 写的编译器来说更便宜。

1. **带载荷的枚举 + `match`。** 放第一个是因为编译器自己就需要它：它的节点种类是整数（`k == 31` 在 `check.vel` 里到处出现），而对一个真正的枚举做穷尽 `match` 能消除一整类静默 bug。验收：`parts/*.vel` 里的 AST 节点种类分派被改写成枚举，而不动点仍然通过。
2. **模块与导入。** 今天一个多文件程序是构建期的拼接（`tools/link_selfhost.vel`）。真正的模块会解锁库、增量构建和跨文件 IDE 导航，并且让编译器自己的各个部件不再是拼接起来的文本。验收：一个两文件程序能编译；自举构建不再需要那个链接器；IDE 能解析定义在另一个文件里的名字。
3. **不带异常的错误处理**：`Result[T, E]` 与 `?`，这样一个可能失败的函数就有一个*类型*说明这件事。需要第 1 项。验收：一个语料用例，其中错误被传播并被处理，没有 panic、也没有退出状态。
4. **泛型（单态化）**，标准库在它存在之前需要这个：`Vec[T]`、`Map[K, V]`。验收：这些容器可用，而且编译器自己的源码至少用到其中之一。
5. **闭包 / 一等函数**，并有一条显式、有文档的捕获规则——arena 意味着一个逃逸的闭包就是一个悬垂栈帧，所以一个活得比它的栈帧更久的闭包必须**由类型系统**拒绝，而不是由崩溃拒绝。验收：一个接受函数参数的函数可用；一个逃逸的闭包是点明理由的编译错误。
6. **一个标准库**：`Vec`、`Map`、`str` 方法、格式化，以及编译器已经需要的那些文件与进程调用，全部用 Vela 写。编译器是第一个客户，就像它一直是其他一切的第一个客户那样。
7. **迭代与切片**：`for x in xs`、作为值的 range、切片。
8. **trait / 接口**，如果有一天泛型需要约束才能用得起来。
9. **直接执行，以及它的交互形式。** 所有者已经两次声明过这一点，而把它写成一项要求而不是一个偏好是值得的：一个 Vela 程序必须能在不产出任何其他语言的文件、也不先编译出可执行文件的前提下*可运行*。`vm.exe run file.vel` 已经精确满足这一点——它就是解释器：没有 C、没有 `.ll`、没有 `.exe`，程序旁边什么都不写——语料里的每个 `run` 用例都*两种方式*执行，正是为此。**不**存在的是交互形式：一个 REPL，在那里敲一行就被求值。它需要这门语言有意缺少的那一项宿主能力——读标准输入（`SPEC.md` §8 列出了宿主表面，而那里没有 `read_line`；调试器也出于同样的理由不得不改用命令文件），这使它成为一项*语言*改动而不是驱动改动，所以它属于这里，而不属于 CLI 那一节。验收：`vm.exe repl` 在提示符下求值键入的内容；`run` 仍然只需要 `vm.exe`；而 README 里关于"直接"运行的句子明确点名解释器，而不是留读者去假设走的是编译路径。

有意**不**在这张清单上的，是为了让这门语言保持纯血和小的东西：GC（arena 加值语义就是设计）、`null`（它不存在，也不应该存在）、作为文本替换的宏，以及任何形式的 C++ 依赖。

## 工作线 3 —— 安全性，完成并*被证明*

1. **补上检查器的洞**，让 `check` 和 `build` 一样强：绑定的类型比较、调用实参的比较、未声明的类型名由检查器拒绝。验收：在全部四个实测的洞上，`check` 与 `build` 给出相同的判定和相同的消息。
2. **完成 `parallel for` 的别名规则**并证明它的五个判定：跨迭代的读被拒绝，`out[i] = a[i] * 2` 仍然合法并照旧发出 `#pragma omp`，`a[i] = a[i] + 1` 仍然合法。
3. **一个安全性语料**：对抗性用例（竞态、别名、溢出、边界、生命周期），它们的 golden 记录的是*诊断*，所以一次回归就是一次"本该发生却没有发生的拒绝"。
4. **三路差分测试**（C 后端、LLVM 后端、解释器），覆盖每一个 `run` 用例，逐字节，作为套件的永久成员。

## 工作线 4 —— 围绕它的工具，正是让它可用的东西

1. **版本控制——已完成，而这一条是"为什么它排第一"的记录。** 写它的时候这个仓库还没有版本控制，而它已经因为一次 `del` 丢过一个测试语料，只因为会话记录恰好留着一份旧测试文件的副本才找了回来。`git init` 和第一次提交不花什么代价，却移除了上面一切所面临的最大单一风险。现在它是 `main`，在 GitHub 上有 `origin`（`git rev-list --count HEAD`，2026-09-22 实测），所以这一条点名的风险已经关闭；把"这个仓库没有版本控制"留在清单里，就会让一份已经执行了的计划读起来像还没开始。
2. **IDEA 插件**（独立工作线，已在进行中）：语法、诊断、补全、悬停、参数信息、跳转、重命名、结构视图、AST 窗口、运行配置、格式化、折叠、注释、模板，以及一个配色设置页——每一项注册都对着平台自己的声明验证过，因为这个插件里已经有三个特性因为少一行而发布后就是死的。它的 Run 按钮**解释执行**（`vm.exe run <file>`）：没有构建步骤、程序旁边不写可执行文件、不需要 C 工具链，而且命令行、程序输出和退出状态全都进控制台。这与工作线 2 的第 9 项是同一个要求，只是从编辑器这一侧看。
3. **一个调试器**：编译器的 `debug` 模式存在于源码中；在它被晋级、并有 IDE 能驱动的协议之前，Debug 保持诚实地被拒绝，而不是被伪造。
4. **一条命令跑完一切**：构建、套件、基准、差分测试，以及一份报告——这样"它是不是绿的"就永远不是一个看法问题。

## emitter 的下标重复——一个丢失的断言的实测原因

2026-09-19 实测，此时运行时的边界与溢出检查终于能工作，基准框架终于能跑（`tools\bench.ps1`）：

| 变体，全部都打印同一个答案 | 秒（最好 / 中位数） |
|---|---|
| 串行 matmul 512²，Vela 按 emitter 写出的样子 | 1.058 / 1.066 |
| 同一份发出的 C，下标提到一个临时变量里 | 0.550 |
| 同上，边界检查也去掉 | 0.514 |
| 下标不带检查，边界检查保留 | 0.177 |
| 什么都不检查 | 0.181 |
| C++ 孪生版本 | 0.212 / 0.213 |

emitter 把每个数组下标表达式写了两次——一次在 `vela_bounds_check(...)` 里，一次在下标里——而下标表达式本身就是 `vela_mul_range` + `vela_add_range`，所以每次元素访问都会跑两遍带检查的算术，而 MSVC /O2 不会合并这两个副本。这在 1.06 s 里值 0.51 s。

**这张表是一次更正，值得把它当作更正来读。** 本节的第一版把那个手工编辑的变体放在 0.176 s，并得出"这些检查不花什么代价"的结论——但那个变体提出来的是*不带检查*的下标算术，只保留了边界检查，所以它的标注（"index hoisted, checks kept"）并没有描述它所测量的东西，而那个结论是假的。第二次隔离同一项代价，得到了上面那个五行拆分。按它们改变"接下来做什么"的程度排序的**四个**后果——这里过去写的是"三个"，而下面列着四条，那是一种读者不数一遍就查不出来的算术：

1. **修法是让 emitter 只计算每个下标一次**（一个临时变量，就像上面手工编辑的 C 那样）。验收：基准里 Vela 的串行 matmul 达到 **0.6 s 或更好**——预期落在约 0.55 s，所以这条线是个下限而不是目标——同时它的答案不变、`tools\refreeze.ps1` 仍然是 190/190 的 `RESULT: green`、不动点仍然逐字节相同、并且一个越界探针仍然带着同样的消息和行号 panic。
2. **在那次修复之后，带检查的下标算术是剩下的最大代价**：如果下标上的 `vela_mul_range`/`vela_add_range` 调用消失，就从 0.550 s 降到 0.177 s，那是剩下部分的约 68%，也是"输给 C++ 孪生版本"与"赢过它"之间的差别。这正是 `selfhost/ELISION_PLAN.md` 的用途，而这张表就是它的理由——这份计划值得在第 1 步*之后*做，而不是取代它。
3. **边界检查本身很便宜**（去掉它就是 `bench/RESULTS.md` 那张阶梯表里的一行，价值 `0.543 − 0.514 = **0.029 s**`；这一条过去引用的 `~0.04 s` 无法从那张阶梯表里推出来，所以它被去掉了，而不是重新猜一个），所以消除计划里 `NF_NO_BOUNDS` 那一半是较小的一半；`NF_NO_OVERFLOW` 才是时间所在。
4. **一条断言的好坏只取决于最后一次测量。** README 的这一行曾经说"在串行 matmul 上取胜"，说了一天，而发出的代码实际上比手工变体慢了六倍。框架现在是一条命令，所以重新取这个数字很便宜；引用它之前先取一次。

## 每条断言将由什么来判定

| 断言 | 判定它的命令 |
|---|---|
| 比 C++ 更快 | `powershell -ExecutionPolicy Bypass -File tools\bench.ps1 -Reps 7`——`bench\RESULTS.md` 里的表，连同每个变体必须打印的答案。**目前在 matmul 上为假，且原因已实测**：C emitter 把每个数组下标表达式写了两次（见下面 §"emitter 的下标重复"）。这条断言由那被修好之后的数字决定，而不是之前 |
| Rust 的安全设计 | 安全性语料：每个用例记录的诊断，加上三路差分 |
| Python 的语法，严格的语义 | 在四个实测的洞上跑 `vm.exe check`，加上语料里的 55 个拒绝用例 |
| 完整的高级特性 | 工作线 2.1–2.8，每一项都带上文的验收行 |
| 纯血 | 在 `PATH` 上任何地方都没有 `cl.exe` 的情况下 `vm.exe build selfhost\vm.vel` |
