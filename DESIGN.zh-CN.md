# Vela —— 设计笔记

[English](DESIGN.md) | **简体中文**

<!--
源文件 : DESIGN.md
源文件字节 : 64814
源文件 SHA256 : afe6ccd53dacf7347d47c6c2ffe09ce88927ad6ef9d5ecf3139fd89de2420edf
翻译日期 : 2026-09-22
规则 : 本文件是上面那个英文文件的完整翻译。英文文件一旦改动，本文件立即过期，
       powershell -ExecutionPolicy Bypass -File tools\docs-zh-check.ps1 会指名报告。
-->

这门语言为什么被塑造成现在这个样子，以及各个部件如何咬合在一起。这是改动任何东西之前该读的文档：下面大多数不寻常的决定都是承重的，其中几个直接来自构建它时发现的 bug。

---

## 1. 三条断言，以及每一条是如何被变成真的

### 1.1 "比 C++ 更快"

这条断言不是"Vela 有更好的优化器"——它没有。这条断言是：**一门语言可以把 C++ 编译器只能靠猜的事实交给 C 编译器**，而那些事实比手写后端能添加的任何东西都更值钱。

| Vela 知道的事实 | 它换来什么 |
|---|---|
| 类型里带着精确长度 | `len()` 是常量；没有长度记账 |
| 算术永远带检查 | 这些检查是 C 编译器能看穿的调用（`vela_add_range`），而这个编译器里没有任何开关能去掉它们 |
| 一个迭代空间是无数据竞争的 | 一个真正的 `#pragma omp parallel for`——只在有证明时发出，且只有当 C 里带有 pragma 时才把 `/openmp` 传给 C 编译器 |

这张表曾有两行是意图而不是代码，它们在这里被点名而不是被假定：**数组永不互为别名，但后端还没有发出 `restrict`**，以及**区间分析还不会删除边界或溢出检查**——它今天换来的是 `parallel for` 的证明，以及对一个不可能落在范围内的常量下标的拒绝（§6.1、§7.5）。在此期间，发出的 C 是有意保守的。

底层是 C11（MSVC 上 `/O2`，gcc/clang 上 `-O3`），所以寄存器分配、向量化和指令选择都取决于一个几十年前的优化器。这是有意的：与 LLVM 的后端竞争并不是一门语言为了快而必须做的事，而假装不是这样，正是业余编译器最终变慢的原因。

实测结果在 `bench/RESULTS.md` 里，它报告每一个变体而不是一个分数：串行 matmul 512² 比 C++ 串行孪生版本**慢 2.4×**（0.5433 s 对 0.2241 s），而 OpenMP 的 matmul 比同样使用 OpenMP 的 C++ 构建**慢 3.1×**（0.0680 s 对 0.0221 s），两者都是因为 Vela 的内层循环保留着它带检查的下标算术，而每一行都带着它必须打印的答案，所以一个快而错的答案无法通过。（本段所取代的那一段说串行 matmul 比 C++ 快，又说并行那一行大约落后 17%；两个数字都是错的，而且错的方向相反。第一个把功劳给了 `restrict` 和已被证明消除的边界检查，而编译器两样都没做；第二个早于 2026-09-19 那处 emitter 修复。以上数字是 2026-09-22 在 `bench/RESULTS.md` 中实测的，本段引用的就是那里的行。）

### 1.2 "比 Rust 更安全"

Rust 的安全是所有权纪律加 `unsafe`。Vela 的安全是算术：每一条规则都是一个证明义务，由一个足够小、可以审计的检查器来履行——`selfhost/parts/check.vel`，一个文件——而且没有任何构造可以用来退出。

* 一个 arena，没有 `free` → double-free 和 use-after-free 无法表达；
* 任何函数都不能返回自己的数组 → 没有指针活得比它的栈帧更久；
* `str` 要么是字面量，要么是宿主的永久内存 → 返回的字符串不可能悬垂；
* 检查只靠证明被移除，永远不靠开关或属性——而且这里没有开关：这个编译器没有 `--fast-int`、没有 `-O`，也没有任何别的能关掉检查的东西，所以"快但未定义"也不在选项里；
* 除以常量零、以及常量下标越界，都是编译错误。

使用者真正感觉得到的那些安全规则——默认不可变、没有隐式转换、没有真值性、没有静默无用的表达式语句、没有 `elif`、没有元组——是严格，不是安全，它们在 `SPEC.md` §4 里。

### 1.3 "语法像 Python，但更严格"

语法保留 Python 的表面（可读的词、`for i in range(...)`、*实践中*的缩进，因为人们本来就会缩进），并去掉 Python 的歧义：

```
indentation   -> braces: structure is visible, refactoring cannot silently re-nest
implied types -> every parameter and return annotated, always
truthiness    -> conditions must be bool
rebinding     -> immutable by default, mut to opt in
"a" "b"       -> refused: no implicit concatenation
0 < x < 10    -> refused: chained comparison
x + 1         -> refused when it is a statement: it does nothing
```

**为什么最终是花括号，而不是缩进。** 两个理由，其中一个对这个项目是决定性的：

1. 缩进是*看不见的结构*。任何重写文件的工具（格式化器、迁移脚本、补丁）都可能意外改变含义。就在这个仓库里，一个格式化器的 bug 把 `total += i` 变成了 `total + i`；有了花括号，同一类 bug 会被编译器抓到，因为那条语句肉眼可见地什么都不做——而修复那次损伤的修复脚本已经和它所归属的迁移工具一起消失（§6），让这一类 bug 可见的那次拒绝却留了下来：它是语料里的一个用例。
2. **一个自举的词法分析器不需要缩进栈。** Vela 打算编译自己，而每一件它不需要的机械装置，都是它不必用 Vela 重新实现的机械装置。`selfhost/vela.vel` 的第一版带着一个缩进栈、若干空行特例和 INDENT/DEDENT token；现在这一版这些一个都没有。

---
## 2. 流水线

```
  source.vel
      |  selfhost/vela.vel        tokens (no INDENT/DEDENT at all)
      |  parts/parser.vel         AST: one node pool, stride 10, chains linked
      |  parts/resolve.vel        scopes, frame slots, which function a call reaches
      |  parts/check.vel          types + interval analysis + safety proofs
      |  parts/emit.vel           C11 (arena, checked arithmetic, omp only on proof)
      |  MSVC / gcc / clang       machine code
      v
   executable  (file.c is kept beside the source: it is the evidence for the proofs)
```

`selfhost/build/vm.exe` 就是整条流水线装在一个二进制里——每个方框一个模式（`lex`、`parse`、`check`、`emit-c`、`build`），外加解释器的 `run` 和工具的 `count`/`nodes`/`debug`——而它就是这棵树里的编译器。`tools/link_selfhost.vel` 把各个部件拼接成 `selfhost/vm.vel`，因为 Vela 0.1 没有模块系统（§7.1）。

### 2.1 为什么 stage 0 用 Python

引导一门语言总是从另一门语言开始：Rust 从 OCaml 开始，Go 从 C 开始，Swift 和 Zig 从 C++ 开始，Nim 从 Pascal 开始，C++ 从 C 开始。编译器的宿主语言**对它产出的代码的速度没有任何影响**——这是设计必须诚实回答的问题，而基准回答了它。

选 Python 是因为前端才是有意思的工作所在（类型、证明、诊断），它可以被快速写出和改动，而最终产出的二进制出自 C++ 用户所用的同一个 C 编译器。

Stage 4 把它删掉了，而这正是设计里必须说出来的一部分：这个仓库里**没有 Python**——`vela/`（词法分析器、解析器、检查器、emitter、CLI）、`vela/ide/`、Python 构建工具，以及为这次移植把关的那些差分套件全都走了，`Get-ChildItem -Recurse -Include *.py` 什么都不返回。Python 回答过的一切现在都由 `vm.exe` 回答；一个*编辑器*所需的东西，靠运行编译器来回答，而不是靠这门语言的第二个实现（§5）。

---

## 3. 检查器

### 3.1 区间分析（`selfhost/parts/check.vel` 里的 `ck_iv_*`）

一个 `Iv` 就是一个区间：一对 `lo`/`hi`，每一端各带一个标志，因为"至少 0，上界无界"必须能被表达——而一个带运行期边界的范围给出的正是这个。每一个整数表达式都会得到一个，而一个会溢出的边界会被夹成"无界"而不是回绕，所以证明永远不会建立在一个说谎的界上。

今天这些区间的*用途*是许可与拒绝，而不是删除：它们是允许一个 `parallel for` 的东西（§7.5），也是拒绝一个不可能落在范围内的常量下标的东西（`SPEC.md` §6.1）。靠证明删除检查是设计过但未构建的——它诚实的现状在 `SPEC.md` §6.1 里。

最重要的一条规则是**循环入口处的失效（invalidation）**：在分析任何循环体之前，循环体内被赋值的每个变量都变成未知，然后循环条件再把它能收窄的收窄。这正是让消除在后向边上仍然可靠的东西；没有它，`i` 就可能被假定为入口处的值，而编译器会兴高采烈地删掉第二次迭代所需的那项检查。

离开一个块（`pop`）会把一个脏集合向外传播，所以在某个分支内部发现的事实永远不会泄漏到分支之外。

### 3.2 边界检查

长度是数组类型的一部分，所以 `ck_index` 把一个*常量*下标与它比较，并在下标不可能落在范围内时拒绝这个程序——这就是语料里 `const_index_out_of_range` 和 `negative_const_index` 所承载的东西。其他每一个下标都在它被使用的地方检查：`vela_bounds_check(i, N, file, line)` 会带着下标、长度和它来自的那一行 panic。Stage 0 靠证明删除了这些检查，`SPEC.md` §6.1 保留了那些证明的表；这个后端还一项都没删，这就是发出的 C 有意保守的原因。

### 3.3 并行证明（`check.vel` 里的 `ck_parallel`）

见 `SPEC.md` §7。这些规则之所以存在，是因为失效模式是静默的：matmul 基准的 C++ 孪生版本在内层下标被线程共享时产出了垃圾（`checksum 576295424`），而 C++ 里没有一个字说话。这个证明也是后端 `#pragma omp parallel for` 所依托的东西——pragma 只为它接纳的循环发出，而驱动会在把 `/openmp` 传给宿主编译器之前先问发出的 C 里有没有带着它（§7.5）。

**这条规则从哪来，以及它有意不做什么。** 证明中别名的那一半——循环体写入的数组，在该循环体里只能在下标表达式是写入自身下标表达式的地方被读——是 Rust 的 `par_iter_mut` 编码在它*类型*里的借用规则在语言层面的形式：每一次迭代都拿到自己的 `&mut`，所以没有哪次迭代能够触及别人的元素。Vela 没有生命周期来拼写这件事，所以检查器对它正在看的那个循环证明同一件事，而代价是一次拒绝，而不是一门被削弱的语言：被写数组的下标表达式只允许一个，而不是每个数组一个。只读数组仍然可以随意下标——`out[i] = a[i] * 2` 正是这条规则要保住的形状，而它编译成一个真正的 `#pragma omp parallel for`，其中 `a` 被每个线程读取。被拒绝的是循环携带依赖：`a[i] = a[i - 1] + 1` 读的是邻居正在写的东西，在它之上 OpenMP 每次运行都会算出不同的答案（实测：64 元素前缀和的二十次运行里有九个不同的错误值，而解释器给出 63）。拒绝它是这次移植自己的决定；stage 0 发出同一个循环时不带 pragma，因此也没有竞态，这就是为什么没有任何 golden 覆盖这个形状。

这个证明有两个限制被点名而不是被藏起来：比较是*结构性的*（`ck_same_expr`），所以 `a[i]` 和 `a[i * 1]` 被当作不同的表达式（拒绝——安全的方向），而一个被嵌套块*遮蔽*的名字与它所遮蔽的循环变量比较时相等，读规则和写规则都一样，因为两者比较的是驻留句柄，而不是解析后的绑定槽位。
---

## 4. 运行时（`runtime/vela_runtime.h`）

一个头文件，没有构建系统，约 450 行：

* **arena**：默认 256 MB（`VELA_ARENA_MB`），16 字节对齐，总是清零——这就是为什么一个裸的数组声明是安全的；
* **frame**：一个声明了数组的函数会取一个 `vela_arena_mark`，并在每一条返回路径上释放它；没有函数可以返回数组，所以没有任何东西能指向一个已释放的 frame；
* **永久区**：来自宿主的字符串（`read_text`、`intern`）住在这里，永不释放，这正是让 `str` 可以安全地返回、让 `substr` 无需所有权跟踪就安全的原因；
* **带检查的算术**：`vela_add_range` 及其伙伴，永远如此。这个编译器根本不发出任何回绕模式——运行时仍然带着 `vela_wrap_*`，而被删掉的 `--fast-int` 模式是唯一曾调用过它们的东西；
* **地板除**：`vela_floor_div_u` / `vela_floor_mod_u`，因为 Python 的 `//` 和 `%` 在负数上不是 C 的行为，而使用者会注意到；
* **宿主 I/O**：`read_text`、`write_text`、`bytes_at`、`substr`、`argc`、`arg`、`intern`/`interned`、`unescape`、`panic`、`emit_*`、`warn_*`。

运行时里没有任何操作有未定义行为，包括那个被删掉的 `--fast-int` 模式所用过的回绕操作。

---

## 5. IDE：浏览器编辑器被删除，IDEA 插件取代了它

Stage 0 交付过一个 Web IDE——`python -m vela ide`，一个只监听回环地址的 HTTP 服务器，架在 `vela/ide/web/` 之上，通过运行真正的编译器来回答 JSON 查询（`/api/check`、`/api/complete`、`/api/hover`、`/api/definition`、`/api/references`、`/api/signature`、`/api/outline`、`/api/format`、`/api/tree`、`/api/file`、`/api/search`、文件操作、`/api/run` 以及一套作业 API），配一个无依赖、没有构建步骤的页面。它跑在 Python 语言服务上，所以 stage 4 把它和其余 Python 的东西一起删掉了：`vela/ide/`、那个页面、以及驱动它的 Python 套件都走了。这棵树里没有 HTTP 服务器，编译器里也没有 `ide` 模式。

现在存在的是 `idea-plugin/`：一个面向 `.vel`/`.vela` 文件的 IntelliJ IDEA 插件——文件类型、高亮，以及来自 `selfhost\build\vm.exe check <file>` 的诊断，画在编译器所指的确切位置。那个 "Vela" 子菜单及其 Check/Build-and-Run 动作被删除了：Run 通过一个 `runConfigurationProducer` 来自 IDEA 自己的 Run 菜单并解释执行该文件，而 Check 与实时诊断重复。有一个决定在删除中活了下来，也正是这个插件之所以薄的原因：

* **编辑器从不发明语言事实。** 插件读编译器那两行的拒绝（一条消息加一个 `file:line`），别的一概不读——没有第二个会漂移的检查器，没有对英文做模式匹配——所以 Vela 强制执行的每一条规则，包括 `parallel for` 的证明，都是免费的波浪线。

被删除的那个 IDE 做过的其余事情（补全、悬停、引用、大纲、格式化）不会由一个重新实现来替代；它们会来自编译器暴露更多自己的答案，而那尚未构建（§7.5）。

---

## 6. 工具

| 工具 | 它为什么存在 |
|---|---|
| `tools/build.ps1` | 整个构建，分六步：从签入的 C 引导 `vm.exe`、链接各个部件、用编译器编译编译器、构建独立词法分析器、检查逐字节相同的不动点，并且（带 `-Suites` 时）运行套件。它的记录落在 `..\vela-build-report.txt` |
| `tools/link_selfhost.vel` | 把自举的各个部件拼接成 `selfhost/vm.vel`（模块的替代品）。它由 `vm.exe build` 构建、然后被运行，所以这个链接器也不需要 Python |

干这些活的 Python 工具——格式化器（`velafmt.py`）、缩进转花括号的迁移器、`+=` 修复脚本、`link_selfhost.py`、`bootstrap.py` / `rebuild.py` / `build_selfhost_by_vela.py`——都随着 stage 0 被删除了，`tools/legacy/` 也一样，那是迁移时往返验证用的冻结缩进前端。迁移留下的是 `SPEC.md` 里的语言，不是一个工具：这棵树里除编译器之外没有任何东西能解析 Vela。

---

## 7. 引导计划

| 阶段 | 内容 | 状态 |
|---|---|---|
| 0 | Python 前端 + C11 后端 | **在 stage 4 中删除**：移植是靠着它认证的（对树里的每个程序*以及*每个拒绝用例 70/70 判定一致，其中 42 个消息逐字节一致），然后它走了——`vela/`、`vela/ide/`、Python 工具和差分套件。这棵树里没有东西是 Python |
| 1 | `selfhost/vela.vel`——**用 Vela 写的词法分析器**，逐 token 对着 stage 0 验证过 | 完成，而且它留了下来：这个词法分析器就是在树里的编译器的一部分 |
| 2 | **用 Vela 写的解析器**（`selfhost/parts/parser.vel` + `dump.vel` + `vm_main.vel`，链接进 `selfhost/vm.vel`），逐树对着 stage 0 验证过 | 完成 |
| 2.5 | 用 Vela 写的解释器，所以 `vm.exe run file.vel` 既不需要 Python 也不需要 C 编译器 | **完成**：`parts/{vm_state,resolve,eval}.vel`；语料里的 `run` 用例逐个程序地把它按住编译版孪生的输出 |
| 3 | 用 Vela 写的 C11 emitter（`parts/emit.vel`）以及使用它的驱动：`vm.exe build file.vel` | **完成**：语料里的 `dumps` 用例比较它写出的 C，`native` 用例运行它产出的东西；编译器编译它自己（`vm.exe build selfhost/vm.vel`）；`tools/build.ps1` 第 5 步逐字节检查不动点；而且 `vm.exe build` 自己驱动宿主的 C 编译器，那个脚本所构建的一切都是这样构建出来的 |
| 4 | 用 Vela 写的检查器（`parts/check.vel`），然后把 stage 0 移出工具链 | **完成**：语料里的 `refuse` 用例及其 golden（`tests/golden/*.err`）就是它的记录。`check` 会在每次 `emit-c`、`run` 和 `build` 之前运行（`vm_main.vel`），所以前端会拒绝，而不是为一个它会拒绝的程序发出 C，而 `vm.exe` 是唯一存在的驱动 |
| 5 | 外部函数（只有 C，C++ 有意不在范围内——§9.3） | **完成，是 §9 描述的小形状**：为标量 C 函数提供的 `extern c [pure] def`，在声明处*以及*调用点都被拒绝，解释器里没有外部调用。有意排除在外：块、`cstr`、`(T*, N)` 数组、`extern struct`、可变参数、回调（§9.5） |
已经能用、并且可以运行的东西：

```powershell
powershell -ExecutionPolicy Bypass -File tools\build.ps1          # bootstrap, link, self-compile, fixpoint
selfhost\build\vm.exe parse  bench\matmul.vel                     # the tree, from Vela, in Vela
selfhost\build\vm.exe lex    selfhost\vm.vel
selfhost\build\vm.exe emit-c bench\matmul.vel                     # the C, with its checks and pragma
powershell -ExecutionPolicy Bypass -File tools\build.ps1 -Suites  # ... and the suite
```
### 7.1 stage 2 必须采用的形状

Vela 没有指针、没有泛型、没有堆对象，而且——这一点很重要——**结构体里不能有数组**。一本教科书式的编译器用 C++ 写会用 `vector<Node>` 然后完事；在 Vela 里等价的东西是：

* **步长打包（stride packing）**：一个 `Array[int, 327680]`，节点 `i` 占据 `i * 10 + 0 .. 9`（kind、四个参数、行号、flags、*下一个兄弟*、再加两个），外加给浮点字面量用的 `Array[float, 16384]` 和给类型记录用的 `Array[int, 65536]`；
* **用链而不是打包的孩子**：一个节点的孩子是一条穿过它 `nx` 那个字串起来的链。一串孩子无法被连续地打包，因为一个语句列表是*绕过*它嵌套的块被解析的——嵌套语句是在这中间被追加进同一个池子的。链不在乎这些，而每一次遍历都是一个 `while` 循环；
* **一个只装 int 的 `Ctx` 结构体**，承载每一个游标（位置、行号、token 下标、节点下标、类型池游标、浮点池游标、错误标志）。解析器复用词法分析器的死字段，而不是让结构体长大：`aux` 变成循环深度，所以循环之外的 `break` 仍然被拒绝；
* **显式穿线**：每个递归函数都把它所需的数组作为 `mut` 参数接过来，因为没有全局变量，也没有闭包；
* **一张字符串表**（`intern`/`interned`），所以名字是 int 句柄、名字相等就是整数相等——一个 `str` 无法住进 `Array[int, N]` 里。

这不是对缺失特性的绕行——这是这门语言唯一允许的形状，也是 Vela 实现自己时不需要 `unsafe` 块的原因。`selfhost/vela.vel`（词法分析器）和 `selfhost/parts/parser.vel`（解析器）是这个设计的工作证明。

解释器需要三样这门语言在被解释的子集里没有的东西，其中两样现在已经有了：

* 一个**数组元素的值池**——完成：一个数组值是一个描述符（池标签、基址、长度），而各个池按帧递增分配，所以解释器的内存与编译后程序的内存遵守同一条 arena 规则；
* 一张**结构体的字段布局表**——完成：解析器按声明顺序记录每个结构体的字段，并为每个方法记录它所属的结构体，而这恰好是解析器不记录的信息（`AST.md` §9）；
* 一个**整数宽度转换**——*没有*完成，而且这是语言的缺口而不是解释器的缺口：`b[0]`，其中 `b: Array[u8, …]`，会被检查器接受并由编译后的程序做拓宽（C 隐式拓宽），而这个解释器没有可拓宽进去的宽度——Vela 没有提供 `to_int(u8)`，而一个 `u8` 元素在这里没有值的形状。所以任何带 `Array[u8]` 的程序都被拒绝，连声明一起，并给出那个理由；修法是 SPEC §3.1 里的一条规则（一次宽度转换），而不是改这里，而 §3.1 说明了两个后端今天在宽度上实际做了什么。

### 7.2 差分测试抓到了什么

为这次移植把关的那个 Python 套件（已随 stage 0 删除）用 stage 0 编译每个程序、运行它，并对同一份源码运行解释器，要求标准输出完全一致。在这个测试在场的情况下写解释器，找出了四个无论怎么读代码都读不出来的 bug：

1. **`if` 的 else 字是第 4 个字，不是第 8 个字。** 第 8 个字是 else *计数*（`parser.vel:589`–`:590`）。解释器把计数当成节点下标来读，于是一个带 `if` 而没有 `else` 的程序执行了节点 0——模块节点。
2. **链的终止符不总是 `-1`。** 一个未使用的字被初始化成 `0`，而它不是合法的节点下标，因为节点 0 是模块节点——到处假定 `-1` 是错的，`<= 0` 才是唯一安全的判定。
3. **实参列表并不连续，除非你把它弄连续。** 求值 `f(g(1), 2)` 时会在求值 `g(1)` 的过程中增长栈，所以 `2` 没有落在它旁边，而被调用者是按位置复制参数的。
4. **`break` 必须能抵达循环语句。** 一个在退出路上把 `C_BREAK` 映射成 `C_NEXT` 的辅助函数，就足以把每一次 `break` 变成一次 `continue`。

这些没有一个算异乎寻常；四个全都是那种会让一个自举部件悄悄与原始版本分歧的错误，这也是为什么那个差分测试一直存在到 stage 4 把它删掉为止——而同样的分歧正是今天语料里 `run` 用例盯着的东西（每一个都*被编译*也*被解释*，两者都必须打印 golden 输出）。

### 7.3 编译编译器本身抓到了什么

stage 3 的 emitter 把语料里的每个程序翻译成含义等于 stage 0 那份 C 的 C，然后翻译*编译器*：`vm.exe build selfhost/vm.vel` 写出 `selfhost/vm.c`，宿主的 C 编译器把它变成 `selfhost/vm.exe`，而构建会把它晋级覆盖 `selfhost/build/vm.exe`。必须成立的性质是不动点——它构建出的编译器必须把 `selfhost/vm.vel` 重新发出为同样的 C，逐字节相同——而 `tools/build.ps1` 第 5 步检查的正是这件事（它写出 `selfhost/build/_fixpoint_gen2.c`，并把它的哈希与 `selfhost/build/vm.c` 和 `selfhost/vm.c` 比较；它们今天是相同的，`9CD51090CBCFBF3B`，759 600 bytes）。语料里有一个 `fixpoint` 用例说同样的话。最初抓到这一点的那些 Python 差分套件已随 stage 0 消失；它们做过的检查现在由构建自己做。

语料里的程序本来找不出这里暴露的那个 bug，因为它们没有一个在字符串里放转义：

```vela
print("nl:\nsecond")        # one newline byte, length 1
```

字面量的**值**是解码后的文本；emitter 写的却是它的**原始源文本**（反斜杠和 `n` 都还在），同时把解码后取值的长度交给 `vela_str_lit`。C 随后把这段转义文本读成两个字节，而计数把它截成一个，于是每个带转义的字面量都丢掉了最后一个字节——第一次自举构建在每个该是换行的地方都打印 `` \ ``，在它自己的输出里。修法是一个想法：发出值，以及值字节的计数。语料里的 `string_escapes` 用例现在会在那两者再次分离时失败，而正是这个自举构建让这一类错误变得可见——一个只编译玩具程序的编译器永远不会被问到这个问题。

### 7.4 解释器已知的限制

在这里说明，而不是日后才发现：

* `Array[u8]` 被拒绝（上面那个宽度转换的缺口）。因此 `bench/sieve.vel` 目前还无法在解释器下运行。
* `Array[i32]` 通过 `int32_t` 截断来存储，但没有任何语料程序覆盖它。
* 通过下标进行的复合赋值（`a[i] += 1`）会求值那个*位置*两次。Vela 没有赋值表达式，所以只有当下标表达式调用了非纯函数时这件事才可观察——但它是一个偏离，不是证明。
* 各种限制都报告为干净的拒绝，而不是崩溃：500 个调用帧、1 000 000 000 个解释步、262 080 个栈单元、524 288 个堆单元、2 097 152 个 int 元素、1 048 576 个 float 元素、33 554 432 个字节，以及运行时那张 8192 条的 intern 表（被解释的字符串也住在这里，因为驻留是按内容寻址的，而一个句柄*就是*那个字符串）。
* 自举前端在运行或为程序发出代码之前先检查它（stage 4）：`run`、`emit-c` 和 `build` 都会先调用 `ck_module`，所以检查器拒绝的程序就是一次拒绝，而不是运行期惊喜。检查器尚未证明的东西列在 §7.5。
* **外部调用不是解释器能做到的事。** 它会求值参数，然后对结果什么都不产出：*使用*它会陷入陷阱（`this value cannot be printed`），而一个结果被丢掉的调用——比如说一个为副作用而调用的 `void` C 函数——则静默地什么都不做，副作用也包括在内。前一半是 `tests/golden/extern_c_probe_interp.err` 记录的那条边界；后一半是 `eval.vel` 里的一个缺口，在这里点名而不是藏起来（§9.2）。
* `parallel for` 按顺序执行。一个 `parallel for` 的正确性建立在无数据竞争的证明之上，而一个无竞争循环的顺序执行就是同一次计算；解释器本身没有并行性。

### 7.5 stage 4 究竟留下了什么

写下来是因为"移植完成了"和"移植完成了，除了这些之外"是不同的断言，而只有第二个是真的：

* **`extern` 被 stage 4 留在未实现状态，此后按 §9 描述的小形状建成了**（stage 5）：`extern c [pure] def` 声明一个参数与结果都是标量的 C 函数。`str`、数组和结构体在声明处*以及*调用点都被拒绝，实参的种类会对着声明检查，而解释器依然完全无法做外部调用（§7.4、§9.2）。有意仍然缺失的——块、`cstr`、`(T*, N)` 数组、`extern struct`、可变参数、回调——是 §9.5 的清单。
* **编辑器的服务来自编译器，而那就是 IDE 剩下的一切**（`idea-plugin/`，§5）。那个跑在 stage 0 的 Python 语言服务上的浏览器 IDE 已随 stage 0 删除；插件运行 `vm.exe check` 并画出它说的话。补全、悬停、引用、大纲和格式化是被删除的那个服务的工作，不会由一个重新实现来替代——它们会来自编译器暴露更多自己的答案，而 `vm.exe nodes`（原始节点池）是那件事的开端，还不够。
* **检查器剩下的那些收窄**，全都在安全的方向上（它们可以拒绝一个 stage 0 接受的程序，而永远不会接受一个它拒绝的程序），全都写在 `selfhost/parts/check.vel` 的注释里：`parallel for` 的区间环境持有的是循环体内的那些循环，而不是它被嵌套进去的那些；一个非纯的*方法*只有在接收者是裸名字时才被识别；一个调用的*结果*对 `ck_etype` 是未知的，所以带调用操作数的类型规则会保持沉默（外部调用规则是唯一的例外，而且它读的是它声明登记下来的返回种类，而不是去拓宽 `ck_etype`——§9.2）；而运算符表是按二元运算符检查的，还没有检查一元运算符或比较（`-x` 作用于字符串、`"a" < 1`）。
* **自举 emitter 总是发出带检查的算术——而 OpenMP 现在是它自己的工作量。** 这个编译器里没有 `--fast-int`，也没有任何能关掉检查的东西；反过来说，检查器接纳的一个 `parallel for` 会变成一个真正的 `#pragma omp parallel for`（循环变量声明在循环之外，初始化是一个普通赋值，因为 MSVC 的 `/openmp` 是 OpenMP 2.0），而驱动只在它刚写出的 C 含有 pragma 时才传 `/openmp`——在别处是 `-fopenmp`。
* **证明还没有删除任何一项检查。** 区间分析是允许 `parallel for` 的东西，也是拒绝一个不可能落在范围内的常量下标的东西；发出的 C 带着每一项边界与溢出检查（`SPEC.md` §6.1），也没有任何数组参数被标上 `restrict`。那是后端的缺口，不是规则改变：没有开关能移除一项检查，而把消除加回来是下一个接手后端的人的工作。
* **标量参数上的 `mut` 不会写穿**——这是实现与 `SPEC.md` §3（"`mut` before a parameter means the callee may write through it"）之间的一个缺口，两个前端里都是如此，所以不是两者之间的分歧。`tests/probes/mut_scalar_parameter.vel` 是那个证明，`tests/probes/mut_struct_parameter.vel` 是对照；`selfhost/parts/check.vel` 两个都点了名，而在 2026-09-20 之前**这两个文件都不存在**——那条注释指向的是空气。在 `selfhost\build\vm.exe`（634368 字节）上实测，解释器与 C 后端打印出相同的字节，exit 0，任何地方都没有诊断：

  | 传给一个 `mut` 参数 | 被写回了吗？ | 实测 |
  |---|---|---|
  | 一个标量（`int`） | **没有**——被调用者拿到的是副本 | `scalar after bump: 1`（承诺是 2） |
  | 一个数组*元素*（`a[0]` 传进 `mut int`） | **没有**——同样是副本 | `array element after bump: 5`（承诺是 6） |
  | 一个数组（`mut a: Array[int, 2]`） | 是的——它本来就是指针 | `array parameter after fill: 42` |
  | 一个结构体（`mut b: Box`、`mut self: Box`） | 是的 | `struct field after free function: 2`、`after method: 12` |

  一次被丢掉的写入在这个编译器里不是诊断：它是一个带着 exit 0 的错误数字。而且它不限于那几个探针——`vm.exe debug` 的 `vars` 命令通过 `mut n: int` 参数来数一个栈帧的局部变量，所以那个计数从不回来，`vars` 在一个局部变量明明在作用域内的停止处回答 `locals 0`（§10.6 记录了机制）。调试器是照着规范写出来的代码；不成立的是实现。

  这个缺口扩散到了多远，是实测出来的而不是担心的：扫描 `selfhost/vela.vel` 和 `selfhost/parts/*.vel` 里的每一个 `def`——**624 个函数**——找出会写入标量 `mut` 参数的那个，返回的是**两个**，`dbg_mark` 和 `dbg_walk_decls`，都在调试器的 locals 遍历里。编译器里没有别的代码依赖那次写入传回来，语料里也没有：一个在调用之后读取标量 `mut` 参数的程序读到的是副本，而这件事之所以从未显现，唯一的原因是这样一个程序还从未在这里被写过。
* **前端的池子是固定数组**（`selfhost/vela.vel`）：131 072 个 token、65 536 个语法节点。真实的程序离它们远得很；编译器自己的源码就贴着它们，而这才是 token 池被加倍、而不是被按尺寸定下的诚实原因。一个可增长的池子需要这门语言还没有的一种分配。

---
## 8. 测试

| 什么 | 怎么做 | 它证明了什么 |
|---|---|---|
| 语料（`tests/run_tests.vel`） | 一个由 `vm.exe build` 构建的 Vela 程序；`tests/cases.txt` 每行一个用例，`tests/golden/` 放着每个用例必须产出的东西 | 程序能编译、运行并精确打印它们该打印的东西（`run`、`native`、`panic`），**而且**必须被拒绝的程序带着正确的理由被拒绝，逐字节如此（`refuse`、`refuse-interp`）——包括那些保守的编译器会错误拒绝一个合法程序的用例（`ok`） |
| 同一个语料，`dumps` 用例 | 针对一个文件的 `lex`、`parse` 和 `emit-c` 摘要 | 某一阶段的变化无法藏在另一阶段未变之后 |
| `fixpoint` 用例 | 编译器通过它自己的驱动构建它自己 | 它构建出的编译器写出构建它所用的那份 C |
| `tools/build.ps1 -Suites` | 构建的第 6 步 | 套件作为构建的一部分运行，所以一个弄坏用例的改动会弄坏构建 |
| `bench/run_bench.vel` | 一个由它所测量的编译器构建的 Vela 程序 | 性能，在报告任何时间之前先交叉核对每个变体的答案 |

最要紧的那个用例是*拒绝*的那一半：一门拒绝错东西的语言不是严格，而是坏了。那些曾经把自举各阶段按住 stage 0 的差分套件（逐 token、逐树、逐程序、逐判定）在移植发生期间干了那份活，并已随 stage 0 消失——剩下的是语料和构建自己的不动点，而 `tests/golden/` 记录它们认证了什么。

---

## 9. 外部函数：C 库（0.1 做了什么，拒绝了什么）

需求是：一个 Vela 程序能够*原生地*使用 C 库，而不需要让程序围绕这条边界被重写。C++ 被有意排除（§9.3）。这是唯一一个可能终结安全断言的特性，所以被构建的是配得上 "C 边界" 这个名字的最小东西，而 §9.2 里的每一次拒绝都是一句在 `tests/probes/extern_*.vel` 下有探针撑着的句子。

### 9.1 `extern c` 是什么

```vela
extern c def abs(x: i32) -> i32            # the C prototype `int abs(int)`
extern c pure def sqrt(x: float) -> float  # `double sqrt(double)`, no side effects
```

SPEC §6 规则 1（"no pointers, no unsafe, no FFI"）是让 Vela 的承诺可以被检查、而不是只停留在愿景上的东西。原生的 FFI 会终结它，所以这个承诺是被收窄而不是被放弃：

* **仍然为真：** *在 Vela 源码里*无法表达未定义行为。没有指针类型、没有指针算术、没有 `free`、没有未初始化值、没有强制转换、没有回调——而这条边界所接受的东西里也没有任何一件会把指针放进声明（§9.2），所以一个外部签名不用指针也拼得出来。
* **声明是原型，不是块。** 没有库名要写，也没有头文件要包含：声明*就是* C 原型，声明的名字*就是* C 名字——后端按它的裸名字调用它，没有 Vela 函数会得到的那个 `vl_` 前缀。每个函数一行就是全部的审查面，这与散落在整个代码库里的 `unsafe` 块正好相反。
* **类型就是 ABI 自己那一小把标量。** `int` 是 C 的 `long long`（`int64_t`），`i32` 是 C 的 `int`，`u8` 是 `unsigned char`（`uint8_t`），`float` 是 C 的 `double`，`bool` 是 C 的 `bool`，而 `-> None` 的结果是 C 的 `void`。`i32` 和 `u8` 存在的目的就是让一个 C 原型能被*精确地*写出：声明是否与库匹配是使用者要弄对的事，而编译器只拼写他们写下的东西，不多写一个字。
* **`pure` 是使用者对编译器看不见的副作用所作的陈述。** 一个外部函数正是编译器没有其函数体的那种函数，所以 `extern c pure def sqrt(...)` 就是让一个 C 函数能从 `pure def` 或 `parallel for` 里被调用的东西；没有它，这次调用会像任何其他非纯调用一样被拒绝（`a 'pure' function may not call 'f', which is not declared 'pure'`）。这个词不被验证，也无法被验证——而它是承重的：`pure` 是把一个外部调用放进 `parallel for` 的东西，而在那里，一个确实有副作用的函数会产生竞争。一个错的 `pure` 就是一个错的声明，与一个不匹配库的原型属于同一族（§9.5）。
* **新出现的风险，并且大声说出来：** 库拿给它的东西做了什么。一个 C 函数可以破坏内存，而一个调用 `abort()`、`exit()` 或 `longjmp()` 的函数仍然如此——被解释或编译的 Vela 程序抓不住它，假装能抓住就是在控制流上说谎。Vela 的工作是把这条边界做得小、显式、有类型，并如实说明它检查了什么。

### 9.2 检查器拒绝了什么，以及为什么

这条边界的两半都是点名某一行的拒绝，而那一行正是读者要改的那一行。

**在声明处。** 规则是一句话：**类型是标量**。

* 一个 **`str`** 参数或结果——Vela 的 `str` 是长度加上一个指向 Vela 自己字符串区的指针，不是 `char *`。把它交出去意味着发明一套 ABI（一个 NUL 结尾的副本，而 C 之后可以自由地误读它的长度），而返回来的 `char *` 没有长度，所以 Vela 无法用它做出 `str`：`'extern c' parameter 's' has type str`、`'extern c' function 'name' may not return str`。
* 一个**数组**参数或结果——长度归 Vela 所有的存储。返回一个会把栈帧展开时被释放的内存交给调用者，而传一个会丢掉长度，而长度是 Vela 所知的一半：`'extern c' parameter 'a' has type Array[int, 4]`。
* 一个**结构体**——C 需要一个使用者从未声明过的布局（`'extern c' parameter 'p' has type struct`）。这同样也是**没有外部方法**的原因：一个方法的第一个参数是它的接收者，而一个作用于 `P` 的方法的接收者是一个 `P`，所以方法上的 `extern c` 被同一条参数规则拒绝，指向那个没人写过的参数（`'extern c' parameter 'self' has type struct`）。
* 一个 ABI 无法承载的结果——结果位置上的 `None` 是 C 的 `void` 并被接受（发出的原型是 `void f(int32_t);`）；其他一切就是从另一个方向的同样三种拒绝（`'extern c' function 'name' may not return str`）。

**在调用点。** 同一个承诺的另一半，也是抓住外部调用实际会犯的错误的那个——那是 C 不会替你抓的，因为 C 编译器会把 `int` 转成 `float`、把 `float` 转成 `int`、把指针转成 `int` 而不说一个字，于是 `abs(1.5)` 会打印 1，`abs(some_array)` 会截断一个地址。规则是**一个实参只能以参数所声明的种类穿过**，唯一例外是 SPEC §3.1 给整门语言的那一条：一个值装得下的 int *常量*（`abs(-7)`，以及 `i32 + 1`）。其他一切都是在调用那一行的类型错误：

* `'extern c' argument to 'abs' has type float, but parameter 'x' has type i32`——`abs(1.5)`、`abs(f)`（`f` 是 `float`）、以及 `abs(half())`（`half()` 返回 `float`）：一个调用的结果是已知的，因为每个声明打包的返回种类在任何函数体被遍历之前就登记进了节点表（`ck_record_decls`），所以判定不取决于 `extern c` 那一行在文件里的位置。
* `... has type int, but parameter 'x' has type i32`——一个 Vela `int` 的*值*（循环变量、int 绑定、一个返回 `int` 的 Vela 函数的结果）不会在读者背后被收窄成 `i32`；`... has type int, but parameter 'x' has type float` 就是 `sqrt(4)`，因为 Vela 里没有从 int 到 float 的隐式转换，而 `to_float` 的存在正是为此（工作探针拼的是 `sqrt(to_float(i))`）。
* `'extern c' argument 300 to 'double_byte' does not fit in u8`——那个常量是可见的，无法在转换中存活。一个对 `i32` 来说太大的常量会被上一步的规则抓住（`abs(2147483648)` 被报告为 `has type int, but parameter 'x' has type i32`），因为 `ck_foldable` 把这一趟会折叠的东西限制在 2^30——两个判定都是对的，且没有一个是静默的。
* 数组、结构体、`str` 和 `bool` 是同一种形状：消息把两个种类都拼出来，而数组会带着它的长度一起拼（`Array[int, 4]`）。

**参数个数**是解析器的判定，与一个寻常的 Vela 函数得到的是同一个——`vela: type error: wrong number of arguments`，在调用那一行。那条消息是 stage 0 的、逐字节如此，不是这条边界可以改写的。

**解释器。** Vela 没有 FFI，也不打算长出一个，而这件事正是这样显形的：被解释的程序没有办法调用 C 函数。解释器求值参数，然后对结果什么都不产出，所以*使用*它会陷入陷阱（`this value cannot be printed`），而一个结果被丢掉的调用则静默地什么都不做——副作用也包括在内。前一半是 `tests/golden/extern_c_probe_interp.err` 记录的承诺；后一半是缺口，不是承诺，它在 §7.4 里被点名。

### 9.3 C++ 有意不在范围内

C++ 没有稳定的 ABI：名字修饰、`std::` 布局、内联与模板实例化、虚派发和异常都是按编译器而异的。直接够到一个 C++ 类意味着用一个 `this` 指针调用一个被修饰过的符号并跟踪一个 vtable——那把裸指针放进这门语言，够不到内联或模板成员，并且把一个编译器的 ABI 硬编码进去。替代方案（为每个库生成一个 `extern "C"` 包装）对一条已经有稳定答案的边界来说是实打实的工作：把包装写一次，用 C 写，然后通过上面那条 C 路径调用它。

所以决定是：**C 库直接接；C++ 库通过它们暴露出来的任意 C ABI 接。** 不调用被修饰的符号，不生成 C++ 胶水，不为 Vela 并不建模的语言添第二个前端。一个想被 Vela 使用的 C++ 库要发布（或者获得）一个小 `extern "C"` shim，而那个 shim 就是一个 C 库。

### 9.4 为什么宿主表面在 stage 3 为三个调用而变大

`vm.exe build file.vel` 必须发出 C、然后让一个 C 编译器编译它，而 Vela 完全没有启动进程的办法。这件事值得小心，因为它是唯一一个可能悄悄终结安全断言的特性——所以表面恰好增长了三个调用，每一个都回答一个这门语言否则问不出的问题，每一个都在 SPEC §8 里有文档：

| 调用 | 为什么更小的东西不行 |
|---|---|
| `concat(a, b)` | 一个驱动必须能命名文件并构造命令行。Vela 拒绝在两个字符串上用 `a + b`（SPEC §1.5），而 `substr` 只能裁剪，所以没有*制造*字符串的办法，驱动就根本无法用 Vela 写出。它是一个具名内置函数而不是运算符，所以代价始终在调用点可见。 |
| `env(name)` | 驱动必须知道它是否在 Windows 上、以及使用者想要哪个 C 编译器（`VELA_VCVARS`、`VELA_CC`）。未设置和空给出同一个答案——`""`——因为 Vela 没有 `None` 可以返回。 |
| `run_command(cmd)` | 把命令行交给宿主的 shell，拿回一个退出状态（`0` = 成功了）。命令自己的输出直接去这个进程的 stdout/stderr，不被捕获。 |

它们没有一个会做这些事：返回指针、返回一个指向这门语言自己内存的句柄、加载库，或者保留任何比调用活得更久的状态。安全 Rust 有 `std::process::Command` 和 `std::env`；这就是那一类表面被削减到它最小可用形状的样子。它也是让*单个二进制*成为整条工具链的原因：`vm.exe build` 以 `emit-c` 模式重新进入自己（shell 做重定向，所以后端保持一个输出目标），通过 `VELA_VCVARS` 或标准安装目录找到 MSVC，并回退到 `$VELA_CC`、`cc`、`gcc` 或 `clang`。让它接受问责的是构建本身：`tools/build.ps1` 用这个驱动构建每一个阶段并检查不动点，而 `vm.exe build selfhost/vm.vel` 是它将被交付的最大程序。

有三个宿主怪癖记在这里，因为它们花掉了真实的调试时间：

* **`cmd.exe /c` 会剥掉一个前导引号和一个尾随引号**，除非整条命令行恰好是一个被引号包起来的可执行文件。这个驱动构建的每条命令都给它的程序和参数加引号，所以在 Windows 上它会把整条命令再包进一对引号里。否则症状是：一个明明存在的路径报 *"The system cannot find the path specified"*。
* **MSVC 按控制台代码页打印诊断。** 一个把它们当 UTF-8 解码的工具会得到替换字符：最初的 Python 套件试过，其中一个死于它自己的报告（在一个 `cp936` 控制台上 `UnicodeEncodeError`），直到它们的流被重新配置成 `errors="replace"`。现在的套件正是为此把 cl.exe 的输出捕获到文件里，而不是在飞行中解析它。
* **两个共用一个构建目录的套件会输掉一场竞态**，并把它报告成后端失败。Python 套件们为此各自把 stage 0 构建进自己的目录；今天的代价是同时跑两个 `tools/build.ps1` 会为 `selfhost/build/vm.exe` 打起来（第二个的 C 链接失败，报 `LNK1104: cannot open ... vm.exe`）。一次只跑一个构建。

Stage 5 在被解释的路径上不需要*桩表*：一门没有 unsafe 表面的语言也没有 `dlsym`，而与其发明一个，解释器干脆完全没有外部调用（§9.2）——这就是语料里 `refuse-interp` 用例所断言的边界。

### 9.4.1 关于本节不是什么的一个注记

§9 讲的是*外部*边界。§10 讲的是调试器，而它直到 2026-09-20 都还缺失，尽管 `selfhost/parts/eval.vel` 按编号引用过它；那一节是照着源码和运行写出来的，不是凭记忆写的。

### 9.5 有意留着不做的东西

§9.1 里的边界比本节最初承载的设计小得多，而这个差别*就是*设计决定，不是疏忽：

```vela
extern c "libm" {                          # the shape that was designed, not built
    def c_sqrt(x: float) -> float
    def c_snprintf(buf: mut Array[u8, 256], n: int, fmt: cstr, ...) -> int
}
```

* **没有库名，没有头文件，没有 `extern c "..." { ... }` 块。** 链接步骤属于 Vela 自己的驱动（`vm.exe build` 把发出的 C 交给 cl.exe），而一个 Vela 从不传给链接器的库名会是一个关于 Vela 并不驱动的工具链的承诺。每个函数一条声明说出同样的话，而更不容易出错。
* **没有 `cstr`，也没有以 `(T*, N)` 穿过的 `Array[T, N]`。** 这两条规则会让这条边界对人们真正想要的库（`printf`、`memcpy`）变得有用——而也是这两条会把指针放进 Vela 源码。每一条都需要 Vela 0.1 没有的生命周期与所有权叙事（为一次调用借用？拷贝？谁来释放？）；在那之前，一个非标量边界的诚实拼法是带纯标量原型的 C shim，而这正是探针所调用的那两个函数（`abs`、`sqrt`）已经是的形状。
* **没有 `extern struct`、没有可变参数、没有回调、没有库保留的指针。** 每一个都是一项真实的设计——布局固定、varargs、所有权与生命周期——而它们没有一个是 0.1 的工作。
* **没有比 `u8` 更宽的无符号类型。** 没有 `u32`，所以 `void srand(unsigned int)` 无法被*精确地*声明；声明仍然可以说谎，但编译器不会帮它说谎，而这是标量 C 签名唯一够不到的地方。
* **没有任何东西把声明与库对照检查。** 一个与 C 原型不一致的 Vela 声明是这个编译器看不见的错误——而且同一个名字的两条声明也不会被拒绝：像同一个函数的两个 Vela 定义一样，最后读到的那条获胜，而如果它们的原型冲突，说话的是 C 编译器，用 C 的话而不是 Vela 的。
* **没有宽度可写，所以这条边界把宽度守到字面上。** Vela 没有 `to_i32`/`to_u8`，而一个*绑定*根本不是检查宽度的地方（§3.1）——在*调用*处才是：`int`/`i32`/`u8` 实参被按参数的种类守住，所以一个 Vela `int` 的值不会在一个 C 调用处变成 `i32`。窄的那条路是一个声明类型写明宽度的绑定（`mut n: i32 = big`），而真正的修法是 Vela 0.2 的宽度转换，不是这条边界的一条规则。
* **字段读取、数组元素和方法调用对调用点规则是未知的。** `ck_arg_kind` 用检查器在没有类型池的情况下能看到的东西作答——字面量、已绑定的名字、运算符、被下标的数组，以及（对于一个调用）声明登记下来的返回种类——对剩下的保持沉默，这与这一趟其余部分的诚实程度一致（§7.5）。一个*内置函数*的结果还要更沉默：`len`、`argc` 和 `to_int` 不是表里的函数，所以它们的种类未知，`abs(len(s))` 被接受，由 C 去做收窄。这是这条边界在它宁愿不沉默的地方沉默的唯一一处，而这是安全一侧的沉默：一个检查器说不出种类的值，是它不会去猜的值。
* **外部参数上的 `mut` 被接受，且没有含义。** C 参数是一份拷贝，而参数上的 `mut` 只说明那个（不存在的）函数体可以做什么——与 `SPEC.md` §3 对一个 Vela 标量参数所立的缺口是同一个，而本目录里的探针为这门语言自己的函数记录了它。（这一段曾把它引作 `SPEC:148`，那时规范还是按行编号的；§3 就是那一行所在的节，而这两个名字始终指的是同一个缺口。）

---
## 10. 调试协议（`vm.exe debug`）

同一个解释器，身上挂了一只秒表：`vm.exe debug <file.vel> <cmddir> [program arguments...]`。程序本身没有任何变化——arena、各个池子和 `run_main` 还是同样的调用、同样的顺序——命令行上唯一的新东西是驱动留下它那些命令文件的目录。

### 10.1 这个模式是什么，以及暂停循环

调试器就是带秒表的 `run`：同样的 `parse`、`resolve` 和 `ck_module`，然后是同样的 `run_main`，外加一个会武装解释器语句门（statement gate）的会话（`dbg_session`）。一个行号未被武装的语句——正常情况，也是 `vm.exe run` 下的每个语句——只需付一次对 `vm.dbg_on == 0` 的比较，然后就和没有这一节时一样运行。

每条命令都用 `1`（"给我下一条命令"）或 `0`（"重新跑起来"）回答暂停循环；`dbg_pause` 在答案是 `1` 时循环。`stack` 和 `vars` 是在一次暂停*内部*作答的，这正是它们能在被暂停的栈帧里运行被解释代码的原因，也是 `dbg_gate` 的第一个判定是 `dbg_on == 0` 的原因：那正是阻止调试器调试它自己的东西。`run` 和 `continue` 是同一条代码路径（`dbg_arm_run(vm, 0)`）——`run` 是初始提示所期待的，`continue` 是一次停止所期待的——而 `quit` 在解除武装的会话下恢复运行，这样就不会再有任何东西停下来。

一次停止发生在语句运行**之前**，并且它在事件流上被宣布为 `stopped <line> <reason>`。

### 10.2 线上格式：事件与命令

**事件。**

| 事件 | 何时 |
|---|---|
| `ready` | 每次读一个命令文件之前——这就是提示符 |
| `stopped <line> <reason>` | 一次停止；`reason` 是 `breakpoint`、`step`、`next`、`out` 之一 |
| `breakpoint <line>` | 对 `break <line>` 的确认 |
| `cleared <line>` | 对 `clear <line>` 的确认（对 `clearall` 而言每个断点一行） |
| `locals <n>` | 来自 `vars` 的计数，后面每个局部变量一行 `name = value` |
| `stack <n>` | 来自 `stack` 的计数，后面每个栈帧一行 `<depth> <line> <function>` |
| `exited 0` | 这次运行结束了；退出状态是进程自己的 |
| `error …` | 一条坏命令，或者缺失的命令文件 |

**命令。**

```
break <line> | clear <line> | clearall | run | continue
step | next | out | stack | vars | quit | help
```

`break` 和 `clear` 接受一个行号，`clearall` 忘掉每一个断点，而 `step`、`next` 和 `out` 以一种单步模式恢复运行，使得它们造成的停止带着自己的理由到来。断点是基于行的，并且在会话被武装期间一直存在——除了 §10.6 完整记录的 `continue` 缺陷。

### 10.3 通道

**stdout 只承载被调试程序自己的输出，别的什么都不带；stderr 只承载协议的事件，别的什么都不带。** 这个划分不是装饰：事件走 `warn_*`，那是运行时唯一会 flush 的写出器，所以程序的输出直接按顺序抵达控制台，与它在 `vm.exe run` 下完全一样——这就是为什么这套协议根本不需要 `output` 事件。（`vm_main.vel` 的用法文本声称 "events on stdout"；§10.6 记录了显示它们真正走哪条流的实测。）

**命令以文件到达，而不是从 stdin。** 驱动写出 `<cmddir>\cmd.NNN`——三位数字，`cmd.001`、`cmd.002`、……——从 1 开始编号并按顺序读取，所以一个会话目录既是通道也是抄本：驱动说过什么、按什么顺序说，事后都在磁盘上。一个文件可以放几行；它们按顺序执行，而一行恢复程序运行就结束这一批。

驱动只在它知道想说什么之后才创建第 N 个文件，因为"还没写"和"空"在这里是同一个答案：`read_text` 分不清一个缺失的文件和一个空文件。这同样也是调试器在每次停止时都阻塞、直到下一个文件出现的原因，并且当驱动不再说话时它会说 `error no command file from the driver; running to the end`，然后在解除武装的会话下继续。

三位数字就是全部编号——一个会话有几百条命令，不是几千条——而定宽正是把算术挡在热路径之外的东西。一位数字由 `dbg_digit` 变成文本，而不是由 `interned(48 + d)`，因为一个 intern 句柄只有在*被调试的那个程序*驻留过那个字符串时才有效，而这个调试器的程序是一个编译器：它可能驻留过一把名字，一个数字都没有。

### 10.4 命令行，以及程序自己的参数

`vm.exe debug <file.vel> <cmddir> [program arguments...]`（`SPEC.md` §11）。目录是必需的而不是有默认值，因为一个没有办法听到命令的调试器会在第一次停止时永远阻塞，而这件事诚实的版本是拒绝这条命令行。

程序绝不能看到那个目录：断点不允许改变程序打印或返回的东西，而程序自己的 `argv` 是它所打印内容的一部分。所以 `argc()` 在 `debug` 下少一个——`argc() - 2 - vm.dbg_on`——而 `arg(i)` 通过读下标 `2 + vm.dbg_on` 来跳过那个目录。

### 10.5 它不会藏起来的东西，以及它的限制

一个 float 局部变量显示为 `<float 3>`——它的整数部分，以 float 之名——因为 `emit_float` 是唯一没有 `warn_` 孪生的格式化器。静默地显示一个错的数字会比大声显示一个不完整的数字更糟，而这个取舍在这个模式里处处都以同样的方式做出：`vars` 和 `stack` 打印一个计数，然后精确打印它们有的东西，数组被命名（`<array of 3>`）而不是被展开，而结构体会说明它持有多少个字段。

**限制。**

* **64 个断点**（`D_BP_MAX`），按插入顺序，放在 `D_BP_BASE`（254000）处的保留 arena 字里。
* **栈帧轨迹**同样住在 `HEAP0` 之下，在 `D_STATE_BASE`（250000）处，有 `D_STATE_MAX`（4096）个字，每帧三个字：函数编号、eval 函数编号（`vars` 遍历的东西）、以及这次调用发生的行号。它*不是*解析器的 `SCOPE_BASE` 暂存区，那东西看起来像邻居，但完全不是同类。
* **不使用这个模式时不花任何代价**：每个语句付一次对 `vm.dbg_on == 0` 的比较，然后就和在 `run` 下一样运行。

### 10.6 今天实测坏掉的东西

2026-09-20 对着 `selfhost\build\vm.exe`（634368 字节）实测。其中两个是缺陷，第三个是一份在"该读哪条流"上说谎的文档。

1. **`continue` 会解除它被发出时所在栈帧里的每一个断点。**
   机制是从源码里读出来、然后由运行确认的：`dbg_arm_run`（`eval.vel:2322`）赋值 `vm.dbg_depth = vm.vdepth`——*暂停时*的深度——而 `dbg_bp_hit`（`eval.vel:2265`）在 `vm.vdepth > vm.dbg_depth` 不成立时返回 `False`。所以在 `main` 内部执行的一次 `continue` 会拒绝此后 `main` 里的每一次停止。第一次 `run` 看起来能用，只是因为它是在进入任何栈帧之前、在深度 0 处被武装的。

   那次运行：一个第 4 行会执行三次的程序（一个在里面打印的 `while` 循环），用 `break 4` / `break 5` / `run` 驱动，然后是七个 `continue` 文件。stderr 上的输出，逐字：

   ```
   ready
   breakpoint 4
   ready
   breakpoint 5
   ready
   stopped 4 breakpoint
   ready
   ```

   七条命令里只有一次停止，而且从来不是第 5 行——而它在每次迭代都会被执行。`step`、`next` 和 `out` 不受影响，因为 `dbg_step_stop` 出于自己的理由比较行号和深度；显示单步仍然可用的那次实测序列是 `stopped 5 step`。

   *修法还没写，这里也不断言它。* 深度判定属于单步语义，而防止在刚被恢复的那条语句上再次停下的守卫已经是结构性的——`dbg_gate` 在语句运行之前被调用，而不会为同一条语句重入——但这个读法必须由一个会让断点三次重新触发的测试来确认，以及由那些一旦把守卫简单删除就会坏掉的 `step`/`continue` 交错用例来确认。

2. **`vars` 报告 `locals 0`，而原因是一个语言缺陷，不是调试器缺陷。** 在 `main` 内部的一次停止处，`i` 声明在断点上方两行、并被那条停止的语句读取，`vars` 却回答 `locals 0`——在两个不同的停止处都实测到。机制现在被定位了，靠的是读调用链、然后测量它所依赖的那个语言原语：`dbg_dump_vars`（`eval.vel:2506`）从 `mut n: int = 0` 开始，调用 `dbg_walk_decls(recs, n, ...)`，后者调用 `dbg_walk_body(recs, n, ...)`，后者调用 `dbg_mark(recs, n, ...)`——而计数正是在那里递增的，`n += 1`。这些每一个都是 `mut` **标量**参数，正是这门语言会丢掉的写入（§7.5 带着那两个探针和实测表）。所以计数从不回来，报告循环 `while i < n` 一次都不运行，而命令打印它相信的东西：零。

   因此修法首先属于调试器，而这个取舍是基于一次测量、而不是口味作出的。`selfhost/vela.vel` 和 `selfhost/parts/*.vel` 里的每一个 `def`——**624 个函数**——都被扫描过，找出*写入*标量 `mut` 参数的函数，那是这个缺口唯一能弄坏的形状：**存在两个**，`dbg_mark`（`eval.vel:2428`）和 `dbg_walk_decls`（`eval.vel:2482`），而两者都在这一次遍历里。所以调试器不再依赖那次写入传回来——一个局部的、两个函数的改动，对这门语言的调用约定没有任何影响——而标量上的 `mut` 会在它自己的那个改动里开始意味着 `SPEC.md` §3 所说它意味着的东西，背后有套件和那两个探针，因为那个改动确实会动到这个编译器接受过的每一个程序里的每一个标量 `mut` 参数。

   现在被记录下来、而以前没有记录的：这个缺口恰好扩散进了一个特性，那个特性是使用者看得见的，而它下面的那个原语有了自己的探针，所以下一次尝试从一个测量开始，而不是从一段文字开始。

3. **用法文本点名了错误的流。** `vm_main.vel` 打印 "events on stdout"；在把两条流重定向到不同文件之后，每一个事件都落在 stderr 文件里，而 stdout 文件里只有程序自己的字节。一个为事件读 stdout 的编辑器看到的是程序的输出，完全没有协议。

`SPEC.md` §11 和 `ROADMAP.md` 的 stream 4.3 都把此模式当作*还不能让编辑器来驱动*，而在第 1、2 条修好、并且有一个框架端到端驱动这套协议之前，那个判定成立。已经改变的是：这个判定现在基于两个各自带一份复现的具名缺陷，而不是基于"缺少一节描述协议的文档"。

